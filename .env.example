# Meeting Intelligence — local overrides
# Leave OPENAI_API_KEY unset to use deterministic mock LLM (default).

# OPENAI_API_KEY=
LLM_PROVIDER=mock
CONFIDENCE_THRESHOLD=0.75
DATA_DIR=data
OUTPUT_DIR=artifacts/runs
CHECKPOINT_DB=artifacts/checkpoints.sqlite
